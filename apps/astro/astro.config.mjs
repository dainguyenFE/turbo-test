// @ts-check
import { defineConfig } from "astro/config";

// Served at / on the Astro deployment (e.g. …vercel.app/blogs). On the main Next domain,
// `apps/web` rewrites /blogs and /_astro to that deployment.
// https://astro.build/config
export default defineConfig({});
