/** @type {import('next').NextConfig} */
const nextConfig = {
  // Required when serving this app under /docs on one domain (Vercel multi-zone + web rewrites).
  basePath: "/docs",
};

export default nextConfig;
